<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Needed to actually show the twice-daily reminder notification on
         Android 13+ (API 33); the app also asks for this at runtime the
         first time Home is shown (see MainActivity / ReminderScheduler). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Required by the Google Mobile Ads SDK to fetch and show the
         rewarded ad that now gates the Hint button (see ads/AdsManager.kt). -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Android 11+ (API 30) package visibility: without these, the
         "Rate LoopLine" (Play Store) and "Contact / Feedback" (email)
         rows in Settings can silently fail to find a matching app on some
         devices, even when one is installed. -->
    <queries>
        <package android:name="com.android.vending" />
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.LoopLine">

        <!-- Real Mentric Studios AdMob App ID (LoopLine). -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-9019700052213764~7911035754" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.LoopLine">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
